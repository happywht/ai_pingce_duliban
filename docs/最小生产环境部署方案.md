# 最小生产环境部署方案

## 📦 部署目录结构对比

### 开发环境（当前）

```
try3/                              # 开发环境根目录
├── .venv/                         ❌ 开发依赖（删除）
├── __pycache__/                   ❌ Python缓存（删除）
├── .git/                          ❌ 版本控制（生产不需要）
├── .idea/                         ❌ IDE配置（删除）
├── .claude/                       ❌ Claude配置（删除）
├── .pytest_cache/                 ❌ 测试缓存（删除）
├── .spec-workflow/               ❌ 规范工作流（删除）
│
├── frontend/                      ❌ 前端源码（已整合到backend/static）
├── build/                         ❌ 构建文件（删除）
├── scripts/                       ❌ 开发工具脚本（删除）
├── tools/                         ❌ 工具目录（删除）
│
├── docs/                          ❌ 文档目录（可选）
├── sql/                           ⚠️  数据库脚本（首次部署需要）
│
├── backend/                       ✅ 后端核心目录（保留）
│   ├── static/                    ✅ 前端静态文件（保留）
│   ├── backend_service1126.py     ✅ 主服务（保留）
│   ├── app.py                     ✅ 启动入口（保留）
│   ├── config.py                  ✅ 配置管理（保留）
│   ├── advanced_document_parser.py ✅ 文档解析（保留）
│   ├── requirements.txt           ✅ 依赖清单（保留）
│   └── start_service.py           ⚠️  备用启动脚本（可选）
│
├── start_backend.sh               ✅ 启动脚本（保留）
├── start_backend.bat              ✅ 启动脚本（保留）
├── .env                           ✅ 环境配置（保留）
├── .env.sqlserver.example         ✅ 配置模板（保留）
├── .gitignore                     ❌ Git配置（删除）
│
├── logs/                          ✅ 日志目录（运行时创建）
└── *.md                           ⚠️  文档（可选保留）
```

---

## 🎯 最小生产环境部署目录

### 方案一：极致精简版（推荐）⭐

```
ai-review-system/                  # 生产环境根目录
├── backend/                       # 后端核心目录
│   ├── backend_service1126.py     # 主服务文件
│   ├── app.py                     # Flask启动入口
│   ├── config.py                  # 配置管理
│   ├── advanced_document_parser.py # 文档解析器
│   ├── requirements.txt           # Python依赖
│   │
│   └── static/                    # 前端静态文件
│       ├── config.js              # 配置管理脚本
│       ├── config-manager.html    # 配置管理页面
│       └── project/               # 前端页面
│           ├── frontend_improved.html
│           ├── project-detail.html
│           └── ai_pingce_result.html
│
├── .env                           # 环境配置（生产环境配置）
├── .env.example                   # 配置模板
│
├── start.sh                       # Linux/Mac启动脚本
├── start.bat                      # Windows启动脚本
│
├── README.md                      # 部署说明（必须）
└── logs/                          # 日志目录（运行时自动创建）
```

**文件统计**：
- ✅ **必须文件**: 11个
- 📊 **总大小**: ~500KB（不含虚拟环境）
- 📦 **部署包**: ~5-10MB（含依赖）

---

### 方案二：标准版（含文档和数据库脚本）

```
ai-review-system/
├── backend/                       # 后端核心目录
│   ├── backend_service1126.py
│   ├── app.py
│   ├── config.py
│   ├── advanced_document_parser.py
│   ├── requirements.txt
│   └── static/
│       ├── config.js
│       ├── config-manager.html
│       └── project/
│           ├── frontend_improved.html
│           ├── project-detail.html
│           └── ai_pingce_result.html
│
├── .env                           # 生产配置
├── .env.example                   # 配置模板
│
├── start.sh                       # 启动脚本
├── start.bat                      # 启动脚本
│
├── README.md                      # 部署说明
├── CHANGELOG.md                   # 更新日志
├── 统一服务架构说明.md            # 架构文档
│
├── database/                      # 数据库脚本（首次部署需要）
│   ├── init_database.sql          # 建表脚本
│   └── migrate_data.sql           # 数据迁移（可选）
│
└── logs/                          # 日志目录（运行时创建）
```

**文件统计**：
- ✅ **必须文件**: 11个
- 📚 **文档文件**: 3个
- 💾 **数据库脚本**: 1-2个
- 📊 **总大小**: ~600KB

---

## 📋 文件清单

### ✅ 必须保留的核心文件

| 文件/目录 | 大小 | 说明 | 用途 |
|----------|------|------|------|
| **backend/backend_service1126.py** | ~130KB | 主服务文件 | Flask应用+API |
| **backend/app.py** | ~2KB | 启动入口 | 服务启动 |
| **backend/config.py** | ~6KB | 配置管理 | 加载.env配置 |
| **backend/advanced_document_parser.py** | ~15KB | 文档解析器 | PDF/Word/Excel解析 |
| **backend/requirements.txt** | ~1KB | Python依赖 | pip安装 |
| **backend/static/** | ~100KB | 前端静态文件 | HTML/JS |
| **.env** | ~1KB | 环境配置 | 数据库/AI配置 |
| **start.sh** | ~1KB | Linux启动脚本 | 启动服务 |
| **start.bat** | ~2KB | Windows启动脚本 | 启动服务 |
| **README.md** | ~5KB | 部署说明 | 部署文档 |

**总计**: ~263KB (不含虚拟环境和日志)

### ⚠️ 可选保留的文件

| 文件/目录 | 说明 | 何时需要 |
|----------|------|----------|
| **.env.example** | 配置模板 | 生产环境必须 |
| **database/** | 数据库脚本 | 首次部署需要 |
| **统一服务架构说明.md** | 架构文档 | 技术参考 |
| **CHANGELOG.md** | 更新日志 | 版本管理 |
| **backup/** | 备份脚本 | 运维需要 |

### ❌ 不需要的文件

| 目录/文件类型 | 说明 | 原因 |
|-------------|------|------|
| **.venv/** | 虚拟环境 | 生产环境单独安装 |
| **__pycache__/** | Python缓存 | 运行时自动生成 |
| **.git/** | 版本控制 | 生产环境不需要 |
| **.idea/** | IDE配置 | 开发工具配置 |
| **frontend/** | 前端源码 | 已整合到backend/static |
| **scripts/** | 开发工具 | 开发调试用 |
| **tools/** | 工具目录 | 开发辅助工具 |
| **docs/** | 详细文档 | 已有README即可 |
| **build/** | 构建文件 | 打包产物 |
| **test_*.py** | 测试脚本 | 开发测试用 |
| **start_frontend.*** | 前端启动脚本 | 已废弃 |
| **migrate_*.py** | 迁移脚本 | 一次性使用 |

---

## 🚀 部署步骤

### 准备工作

1. **创建部署目录**
```bash
mkdir -p ai-review-system
cd ai-review-system
```

2. **复制核心文件**
```bash
# 复制后端核心目录
cp -r backend/ .

# 复制启动脚本
cp start_backend.sh start.sh
cp start_backend.bat start.bat

# 复制配置模板
cp .env.sqlserver.example .env.example

# 创建部署说明
cp 统一服务架构说明.md .  # 可选
```

3. **编辑生产配置**
```bash
vim .env
```

配置关键参数：
```env
# 生产环境配置
DB_TYPE=mssql
DB_HOST=your-production-db-server
DB_PORT=1433
DB_USER=production_user
DB_PASSWORD=your_secure_password
DB_NAME=ai_doc_review

# AI配置
ZHIPU_API_KEY=your_production_api_key
ZHIPU_MODEL=glm-4.5

# 并发配置
MAX_CONCURRENT_PROJECTS=5
MAX_CONCURRENT_TASKS=3

# 日志配置
LOG_LEVEL=INFO
```

4. **创建README.md**
```bash
cat > README.md << 'EOF'
# 总承包AI智能评测系统 - 生产环境部署

## 快速启动

### Linux/Mac
```bash
./start.sh
```

### Windows
```cmd
start.bat
```

## 访问地址
- 前端页面: http://server-ip:5000/
- API接口: http://server-ip:5000/api/projects

## 配置文件
- 环境配置: .env
- 配置模板: .env.example

## 日志位置
- 运行日志: logs/backend_service.log
- 错误日志: logs/backend_error.log

## 系统要求
- Python 3.8+
- SQL Server / MySQL / PostgreSQL
- 2GB+ RAM
- 10GB+ 磁盘空间

## 技术支持
查看架构文档: 统一服务架构说明.md
EOF
```

### 安装依赖

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
# Linux/Mac
source venv/bin/activate

# Windows
venv\Scripts\activate

# 安装依赖
pip install -r backend/requirements.txt
```

### 启动服务

```bash
# 方式1：使用启动脚本
./start.sh

# 方式2：手动启动
cd backend
python app.py
```

---

## 📦 部署包制作

### 方式一：直接打包（推荐用于快速部署）

```bash
# 创建部署包目录
mkdir -p deploy-package

# 复制核心文件
cp -r backend/ deploy-package/
cp start_backend.sh deploy-package/start.sh
cp start_backend.bat deploy-package/start.bat
cp .env.sqlserver.example deploy-package/.env.example

# 创建README
cat > deploy-package/README.md << 'EOF'
# 总承包AI智能评测系统 - 生产环境部署包

## 快速部署

1. 解压部署包
2. 配置环境变量: cp .env.example .env && vim .env
3. 安装依赖: pip install -r backend/requirements.txt
4. 启动服务: ./start.sh

详细说明请查看: 统一服务架构说明.md
EOF

# 打包
tar -czf ai-review-system-$(date +%Y%m%d).tar.gz deploy-package/
zip -r ai-review-system-$(date +%Y%m%d).zip deploy-package/
```

### 方式二：PyInstaller打包（推荐用于独立部署）

```bash
# 安装PyInstaller
pip install pyinstaller

# 打包后端服务
cd backend
pyinstaller --onefile --name ai-review-backend backend_service1126.py

# 打包后文件在 dist/ai-review-backend.exe (或无扩展名的可执行文件)
```

**优点**：
- ✅ 无需Python环境
- ✅ 单文件部署
- ✅ 启动速度快

**缺点**：
- ❌ 文件较大（~50-100MB）
- ❌ 修改配置需要重新打包

---

## 🔧 部署脚本

### 自动化部署脚本

创建 `deploy.sh`:

```bash
#!/bin/bash
# 自动化部署脚本

echo "========================================"
echo "🚀 总承包AI智能评测系统 - 自动部署"
echo "========================================"

# 检查系统
if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    OS="linux"
elif [[ "$OSTYPE" == "darwin"* ]]; then
    OS="mac"
else
    OS="windows"
fi

echo "📍 操作系统: $OS"

# 检查Python
if ! command -v python3 &> /dev/null; then
    echo "❌ 未找到Python3，请先安装"
    exit 1
fi

echo "✅ Python版本: $(python3 --version)"

# 创建虚拟环境
if [ ! -d "venv" ]; then
    echo "🔧 创建虚拟环境..."
    python3 -m venv venv
fi

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
echo "📦 安装依赖..."
pip install -q -r backend/requirements.txt

# 检查配置文件
if [ ! -f ".env" ]; then
    echo "⚠️  未找到.env，从模板创建..."
    cp .env.example .env
    echo "❗ 请编辑.env配置数据库连接信息"
    exit 1
fi

echo "✅ 配置文件检查通过"

# 创建日志目录
mkdir -p logs

# 启动服务
echo ""
echo "========================================"
echo "🎯 启动服务..."
echo "========================================"
cd backend
python3 app.py
```

---

## 📊 部署对比

| 方案 | 文件数量 | 部署大小 | Python环境 | 启动速度 | 适用场景 |
|------|---------|---------|-----------|---------|---------|
| **源码部署** | 11个核心文件 | ~500KB | 需要 | 慢 | 开发/测试 |
| **虚拟环境部署** | +venv目录 | ~200MB | 独立 | 中 | 生产环境 |
| **PyInstaller打包** | 1个可执行文件 | ~50-100MB | 不需要 | 快 | 独立部署 |

---

## ✅ 部署检查清单

### 部署前检查

- [ ] 服务器Python版本 >= 3.8
- [ ] 数据库服务正常运行
- [ ] 网络端口5000未被占用
- [ ] 防火墙允许5000端口访问
- [ ] 磁盘空间 >= 10GB
- [ ] 内存 >= 2GB

### 部署后验证

- [ ] 服务启动成功
- [ ] 前端页面可访问 (http://server:5000/)
- [ ] API接口正常 (http://server:5000/api/projects)
- [ ] 日志文件正常生成
- [ ] 数据库连接正常
- [ ] AI服务调用正常

---

## 🎯 生产环境推荐配置

### 小型部署（< 50用户）
- **服务器**: 2核4G
- **操作系统**: Ubuntu 20.04 / CentOS 8
- **数据库**: SQL Server Express / MySQL 8.0
- **部署方式**: 源码部署 + 虚拟环境

### 中型部署（50-200用户）
- **服务器**: 4核8G
- **负载均衡**: Nginx
- **数据库**: MySQL 8.0 / PostgreSQL 13
- **缓存**: Redis（可选）
- **部署方式**: PyInstaller打包 + Systemd服务

### 大型部署（> 200用户）
- **服务器**: 8核16G+（集群）
- **负载均衡**: Nginx + 多实例
- **数据库**: PostgreSQL 13 / SQL Server
- **缓存**: Redis
- **监控**: Prometheus + Grafana
- **部署方式**: Docker容器化

---

## 📝 总结

### 最小部署文件清单

**必须文件（11个）**:
```
backend/backend_service1126.py
backend/app.py
backend/config.py
backend/advanced_document_parser.py
backend/requirements.txt
backend/static/config.js
backend/static/config-manager.html
backend/static/project/frontend_improved.html
backend/static/project/project-detail.html
backend/static/project/ai_pingce_result.html
.env
```

**推荐添加（3个）**:
```
.env.example
start.sh / start.bat
README.md
```

### 部署建议

1. **开发/测试环境**: 使用源码部署
2. **生产环境**: 使用虚拟环境部署
3. **独立部署**: 使用PyInstaller打包
4. **容器化**: 使用Docker（推荐云环境）

---

**文档版本**: v1.0
**更新日期**: 2025-12-24
**适用环境**: 生产环境
