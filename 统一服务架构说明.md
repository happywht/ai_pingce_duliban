# 总承包AI智能评测系统 - 统一服务架构

## 🎉 架构升级

系统已升级为**统一服务架构**，前端和后端API整合到一个Flask服务中！

---

## 📊 新架构优势

### ✅ 改造前（双服务架构）

```
前端服务 (8100端口) ←→ 后端API (5000端口)
http.server          Flask
```

**缺点**：
- ❌ 需要启动两个服务
- ❌ 需要打开两个终端窗口
- ❌ 跨域问题需要CORS配置
- ❌ 部署时需要两个进程

### ✅ 改造后（统一服务架构）⭐

```
Flask统一服务 (5000端口)
├── 前端页面 (/project/*)
└── 后端API (/api/*)
```

**优点**：
- ✅ 只需启动一个服务
- ✅ 只需一个终端窗口
- ✅ 无跨域问题（同源）
- ✅ 部署简单（一个进程）
- ✅ 更符合生产环境实践

---

## 🚀 快速启动

### Windows系统

```cmd
# 双击启动
start_backend.bat
```

### Linux/Mac系统

```bash
# 命令行启动
./start_backend.sh
```

**就这么简单！** 一个命令启动全部服务。

---

## 🌐 访问地址

改造后所有服务都在 **5000端口**：

### 前端页面
- **首页**: http://localhost:5000/ （自动跳转到项目列表）
- **项目列表**: http://localhost:5000/project/frontend_improved.html
- **配置管理**: http://localhost:5000/config-manager.html
- **项目详情**: http://localhost:5000/project/project-detail.html?project_id=YOUR_PROJECT_ID
- **评测结果**: http://localhost:5000/project/ai_pingce_result.html?project_id=YOUR_PROJECT_ID&task_id=YOUR_TASK_ID

### 后端API
- **项目列表**: http://localhost:5000/api/projects
- **任务统计**: http://localhost:5000/api/task_statistics
- **启动评测**: http://localhost:5000/api/start_evaluation
- **获取结果**: http://localhost:5000/api/get_result

---

## 🏗️ 技术架构

```
┌──────────────────────────────────────────────────────────┐
│                       用户浏览器                          │
└──────────────────────────────────────────────────────────┘
                            │
                            │ HTTP请求 (localhost:5000)
                            ▼
┌──────────────────────────────────────────────────────────┐
│              Flask统一服务 (backend/backend_service1126.py) │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  ┌────────────────────────┐      ┌────────────────────┐ │
│  │   前端路由             │      │   API路由          │ │
│  ├────────────────────────┤      ├────────────────────┤ │
│  │ @app.route('/')        │      │ @app.route('/api/*')│ │
│  │ → 项目列表页           │      │ → 业务逻辑处理      │ │
│  │                        │      │ → 数据库操作        │ │
│  │ @app.route('/project') │      │ → AI智能评测        │ │
│  │ → 项目详情页           │      │                    │ │
│  │                        │      │                    │ │
│  │ @app.route('/config')  │      │                    │ │
│  │ → 配置管理页           │      │                    │ │
│  └────────────────────────┘      └────────────────────┘ │
│                                                           │
│  ┌────────────────────────────────────────────────────┐ │
│  │          静态文件服务 (send_from_directory)         │ │
│  │  • HTML: backend/static/project/*.html             │ │
│  │  • JS/CSS: backend/static/*.js                     │ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   数据库      │
                    │   AI服务      │
                    └───────────────┘
```

---

## 📁 目录结构

```
try3/
├── backend/                     # 后端服务目录
│   ├── static/                  # 静态文件目录 ⭐新增
│   │   ├── config.js           # 配置管理脚本
│   │   ├── config-manager.html # 配置管理页面
│   │   └── project/            # 前端页面
│   │       ├── frontend_improved.html
│   │       ├── project-detail.html
│   │       └── ai_pingce_result.html
│   │
│   ├── backend_service1126.py   # Flask主服务 ⭐已修改
│   │   ├── 前端路由 (新增)
│   │   │   ├── @app.route('/')
│   │   │   ├── @app.route('/project/<path:filename>')
│   │   │   └── @app.route('/<path:filename>')
│   │   └── API路由 (原有)
│   │       └── @app.route('/api/*')
│   │
│   ├── app.py                   # 服务启动入口
│   ├── config.py                # 配置管理
│   ├── advanced_document_parser.py
│   ├── requirements.txt
│   └── backend_service.spec
│
├── frontend/                    # 前端源文件目录（保留）
│   ├── start_frontend.py        # 原前端启动脚本（已废弃）
│   ├── config.js
│   ├── config-manager.html
│   └── project/*.html
│
├── start_backend.sh             # 统一服务启动脚本 ⭐已更新
├── start_backend.bat            # Windows启动脚本 ⭐已更新
├── start_frontend.sh            # 原前端启动脚本（已废弃）
├── start_frontend.bat           # Windows前端脚本（已废弃）
│
├── .env                         # 环境配置
├── logs/                        # 日志目录
└── docs/                        # 文档目录
```

**说明**：
- ✅ `backend/static/` - 新增的静态文件目录（用于生产）
- 📁 `frontend/` - 保留的原前端目录（用于开发参考）
- ⚠️ `start_frontend.*` - 已废弃的启动脚本（不再使用）

---

## 🔧 技术实现

### Flask前端路由实现

```python
# backend/backend_service1126.py

from flask import Flask, jsonify, request, send_from_directory

# 1. 首页路由 - 重定向到项目列表
@app.route('/')
def index():
    """前端首页 - 重定向到项目列表页"""
    return send_from_directory('static', 'project/frontend_improved.html')

# 2. 项目页面路由
@app.route('/project/<path:filename>')
def serve_project(filename):
    """项目页面路由"""
    return send_from_directory('static/project', filename)

# 3. 静态资源路由
@app.route('/<path:filename>')
def serve_static(filename):
    """静态资源路由（config.js, config-manager.html等）"""
    return send_from_directory('static', filename)

# 4. API路由（原有，保持不变）
@app.route('/api/projects', methods=['GET'])
def get_projects():
    # ... API逻辑
```

### 路由优先级说明

Flask路由按定义顺序匹配，优先级如下：

1. **`/`** - 精确匹配首页
2. **`/project/<path:filename>`** - 匹配项目页面
3. **`/api/*`** - 匹配API接口
4. **`/<path:filename>`** - 匹配其他静态文件

**注意**：`/api/*` 路由在 `/<path:filename>` 之前定义，因此API请求不会被静态文件路由拦截。

---

## 📝 对比总结

| 特性 | 双服务架构 | 统一服务架构 ⭐ |
|------|-----------|----------------|
| **启动命令** | 需要2个终端 | 只需1个终端 ✅ |
| **服务数量** | 2个进程 | 1个进程 ✅ |
| **端口数量** | 2个 (8100+5000) | 1个 (5000) ✅ |
| **跨域问题** | 需要CORS | 无跨域 ✅ |
| **部署复杂度** | 需要部署2个服务 | 只需部署1个服务 ✅ |
| **资源占用** | 较高 | 较低 ✅ |
| **开发便利** | 前端可独立测试 | 需重启Flask ❌ |
| **生产推荐** | ⚠️ 不推荐 | ✅ 推荐 |

---

## ⚠️ 注意事项

### 1. 前端文件更新

**开发阶段**：
- 修改 `backend/static/project/*.html`
- 刷新浏览器即可看到效果（无需重启Flask）

**生产阶段**：
- 直接更新 `backend/static/` 下的文件
- Flask会自动提供最新文件

### 2. API调用无需修改

前端JavaScript中的API调用无需修改，因为现在是同源：

```javascript
// 旧架构（需要完整URL）
fetch('http://localhost:5000/api/projects')

// 新架构（可以使用相对路径）✅
fetch('/api/projects')
fetch('http://localhost:5000/api/projects')  // 也可以
```

### 3. 静态文件路径

Flask会从 `backend/static/` 目录提供静态文件：

```python
# HTML中的静态资源引用
<script src="/config.js"></script>  <!-- backend/static/config.js -->
<link href="/project/style.css">     <!-- backend/static/project/style.css -->
```

---

## 🎯 开发建议

### 开发阶段

如果需要频繁修改前端HTML/JS，建议：

**方案1：直接修改并测试** ✅
```bash
# 1. 修改 backend/static/project/*.html
# 2. 刷新浏览器查看效果
# 3. 无需重启Flask
```

**方案2：保留原frontend目录用于开发**
```bash
# 1. 在 frontend/ 目录开发
# 2. 测试完成后复制到 backend/static/
# 3. 使用脚本同步
cp frontend/project/*.html backend/static/project/
```

### 生产部署

```bash
# 只需启动一个服务
./start_backend.sh

# 访问全部功能
# 前端: http://your-server:5000/
# API:  http://your-server:5000/api/
```

---

## 🔄 迁移说明

如果你之前使用的是双服务架构，现在迁移非常简单：

### 1. 前端代码无需修改

HTML/JS代码无需任何修改，因为：
- 路由路径保持不变 (`/project/*`, `/config.js`)
- API调用路径保持不变 (`/api/*`)

### 2. 更新书签/收藏夹

将原有的书签从 `8100端口` 改为 `5000端口`：

```
旧: http://localhost:8100/project/frontend_improved.html
新: http://localhost:5000/project/frontend_improved.html
```

### 3. 更新启动命令

```bash
# 旧方式（需要2个终端）
./start_backend.sh   # 终端1
./start_frontend.sh  # 终端2

# 新方式（只需1个终端）✅
./start_backend.sh
```

---

## ❓ 常见问题

### Q1: 我还想使用原来的双服务方式怎么办？

**A**: `frontend/` 目录和 `start_frontend.*` 脚本仍然保留，你可以继续使用：

```bash
# 终端1：启动后端
./start_backend.sh

# 终端2：启动前端（使用http.server）
cd frontend
python3 -m http.server 8100
```

### Q2: 修改HTML后需要重启Flask吗？

**A**: 不需要！Flask的 `send_from_directory` 会直接读取文件，修改HTML后刷新浏览器即可看到效果。

### Q3: 如何调试前端代码？

**A**: 与之前一样：
1. 打开浏览器开发者工具 (F12)
2. 修改 `backend/static/project/*.html`
3. 刷新浏览器查看效果

### Q4: API调用会受影响吗？

**A**: 不会！API路由完全保持不变，前端代码无需修改。

---

## 🎉 总结

统一服务架构带来的是：
- ✅ **更简单的启动流程** - 一个命令搞定
- ✅ **更简单的部署** - 一个进程，一个端口
- ✅ **更好的性能** - 减少HTTP连接，无跨域开销
- ✅ **更低的维护成本** - 只需监控一个服务

**开始享受新的统一服务架构吧！** 🚀

---

**文档版本**: v2.0
**更新日期**: 2025-12-24
**架构类型**: 统一Flask服务
